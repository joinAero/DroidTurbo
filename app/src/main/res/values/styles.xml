<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <!-- TabLayout -->
    <style name="AppTheme.Tab.Icon" parent="">
        <item name="android:src">@null</item>
        <!-- tint color for vector drawable -->
        <!--
        Reference:
        * Working with Drawables: http://developer.android.com/training/material/drawables.html
        * 谈谈Android Material Design 中的Tint（着色）: http://segmentfault.com/a/1190000003038675

        Further:
        * Vector drawable: drawable/ic_apps.xml
        * Tint menu item: MainActivity#tintMenuItem()
        * Tint bitmap: however src can not be vector drawable
            <?xml version="1.0" encoding="utf-8"?>
            <bitmap xmlns:android="http://schemas.android.com/apk/res/android"
                android:src="@drawable/ic_search"
                android:tint="@android:color/white"/>
        -->
        <item name="android:tint">@color/tab_icon_color</item>
    </style>

    <style name="AppTheme.Tab.Text" parent="TextAppearance.Design.Tab">
        <item name="android:textSize">@dimen/tab_text_size</item>
        <item name="android:maxLines">1</item>
        <!-- lowercase tab text -->
        <item name="android:textAllCaps">false</item>
        <item name="textAllCaps">false</item>
    </style>

    <style name="AppTheme.TabLayout" parent="Widget.Design.TabLayout">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">@dimen/tab_height</item>
        <item name="tabPaddingStart">@dimen/tab_padding</item>
        <item name="tabPaddingTop">@dimen/tab_padding</item>
        <item name="tabPaddingEnd">@dimen/tab_padding</item>
        <item name="tabPaddingBottom">@dimen/tab_padding</item>
        <item name="tabTextAppearance">@style/AppTheme.Tab.Text</item>
        <item name="tabSelectedTextColor">?attr/colorAccent</item>
    </style>

</resources>
